### 1. 登录拿 token（复制返回的 token，下面要用）
POST http://localhost:8080/api/auth/login
Content-Type: application/json

{
  "username": "testuser",
  "password": "password123"
}

### 2. 不带 token 尝试提交评论（应该 401/403，验证权限拦截生效）
POST http://localhost:8080/api/albums/1/reviews
Content-Type: application/json

{
  "rating": 5,
  "comment": "Amazing album"
}

### 3. 带 token 提交评论（把 YOUR_TOKEN 换成第1步返回的 token）
POST http://localhost:8080/api/albums/1/reviews
Content-Type: application/json
Authorization: Bearer YOUR_TOKEN

{
  "rating": 5,
  "comment": "Amazing album, one of the best of the decade"
}

### 4. 同一个用户对同一张专辑再评一次（应该是更新,不是新建一条）
POST http://localhost:8080/api/albums/1/reviews
Content-Type: application/json
Authorization: Bearer YOUR_TOKEN

{
  "rating": 4,
  "comment": "Actually, changed my mind slightly"
}

### 5. 查看这张专辑的所有评论（不需要登录）
GET http://localhost:8080/api/albums/1/reviews

### 6. 查看专辑详情,确认带了聚合评分
GET http://localhost:8080/api/albums/1

### 7. 删除评论（先用上面查出来的 review id 替换下面的数字）
DELETE http://localhost:8080/api/reviews/1
Authorization: Bearer YOUR_TOKEN

### 8. 无效评分测试（比如传 6，应该被数据库 CHECK 约束拦下来,返回 500 或错误信息）
POST http://localhost:8080/api/albums/1/reviews
Content-Type: application/json
Authorization: Bearer YOUR_TOKEN

{
  "rating": 6,
  "comment": "This should fail"
}