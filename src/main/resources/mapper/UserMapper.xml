<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.bigjackcn.musicreview.mapper.UserMapper">

    <select id="findByUsername" resultType="io.github.bigjackcn.musicreview.entity.User">
        SELECT id, username, password, email, created_at
        FROM app_user
        WHERE username = #{username}
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM app_user WHERE username = #{username})
    </select>

    <insert id="insert" parameterType="io.github.bigjackcn.musicreview.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_user (username, password, email)
        VALUES (#{username}, #{password}, #{email})
    </insert>

</mapper>